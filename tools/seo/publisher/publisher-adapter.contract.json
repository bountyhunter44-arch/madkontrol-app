{
  "version": "1.0.0",
  "description": "Publisher adapter contract for MadCore SEO.",
  "targets": [
    "firestore-draft",
    "firebase-hosting",
    "vps-static",
    "wordpress",
    "future-social"
  ],
  "adapterShape": {
    "name": "string",
    "target": "firestore-draft|firebase-hosting|vps-static|wordpress|future-social",
    "runtime": "node|browser|shared",
    "supportsPreview": "boolean",
    "requiresHumanApproval": "boolean",
    "publish": "async function({ tenantConfig, pages, assets, context })",
    "rollback": "optional async function({ tenantConfig, publicationId, context })"
  }
}
